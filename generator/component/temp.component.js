import template from './<%= name %>.html';
import './<%= name %>.scss';

const component = {
	bindings: {},
	template: template,
	controller: function() {
		"ngInject"

		
		
	}
};

export default component;
