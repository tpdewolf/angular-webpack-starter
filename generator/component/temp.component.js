import template from './<%= name %>.html';
import './<%= name %>.scss';

let component = {
	bindings: {},
	template: template,
	controller: function() {
		"ngInject"

		
		
	}
};

export default component;
